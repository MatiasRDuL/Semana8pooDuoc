package com.mycompany.s8swing.model;

// Clase de interface que declara metodos a manejar por el resto de las clases que compartan el mismo package
public interface Registrable {
    void mostrarResumen();
    String getResumen();
}
